# Enable AOF persistence
appendonly yes
appendfilename "appendonly.aof"

# fsync strategy
appendfsync everysec

# Memory limit
maxmemory 512mb
maxmemory-policy allkeys-lru

# Enable pub/sub (default)
